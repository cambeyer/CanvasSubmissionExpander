{
  "name": "canvas-submission-expander",
  "version": "1.0.0",
  "description": "Takes a zip file created by Canvas representing all student submissions and extracts them into separate folders for grading",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^7.0.1",
    "unzipper": "^0.9.11"
  }
}
